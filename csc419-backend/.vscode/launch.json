{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI (.venv)",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "python": "${workspaceFolder}\\.venv\\Scripts\\python.exe",
      "args": [
        "app.main:app",
        "--reload"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}
